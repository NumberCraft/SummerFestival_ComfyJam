using UnityEngine;
using UnityEditor;

/*[CustomEditor(typeof(BridgeGenerator))]
public class BridgeEditor : MonoBehaviour
{
    public override void OnInspectorGUI()
    {
        DrawDefaultInspector(); // Draw all other variables

        Door myScript = (Door)target;

        if (GUILayout.Button("Randomize Ids"))
        {
            // Call the function in your main script
            myScript.RandomizeIds();
        }
    }
}*/
